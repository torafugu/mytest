<%= form_for(@onecolumnmodel) do |f| %>
  <% if @onecolumnmodel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@onecolumnmodel.errors.count, "error") %> prohibited this onecolumnmodel from being saved:</h2>

      <ul>
      <% @onecolumnmodel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
