<%= form_for(@shichoson) do |f| %>
  <% if @shichoson.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shichoson.errors.count, "error") %> prohibited this shichoson from being saved:</h2>

      <ul>
      <% @shichoson.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :todofuken %><br />
    <%= f.collection_select(:todofuken_id, Todofuken.find(:all), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
