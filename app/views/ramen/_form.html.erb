<%= form_for(@raman) do |f| %>
  <% if @raman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raman.errors.count, "error") %> prohibited this raman from being saved:</h2>

      <ul>
      <% @raman.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
