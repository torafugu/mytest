<%= form_for(@startingmember) do |f| %>
  <% if @startingmember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@startingmember.errors.count, "error") %> prohibited this startingmember from being saved:</h2>

      <ul>
      <% @startingmember.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_date %><br />
    <%= f.date_select :game_date, :use_month_numbers => true %>
  </div>
  <div class="field">
    <%= f.label :players %><br />
    <%= f.select :players, Player.all.map {|player| [player.name, player.id]}, {}, multiple: true, :size => 8 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
