<%= form_for(@imp_raman) do |f| %>
  <% if @imp_raman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imp_raman.errors.count, "error") %> prohibited this imp_raman from being saved:</h2>

      <ul>
      <% @imp_raman.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :price %><br />
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
