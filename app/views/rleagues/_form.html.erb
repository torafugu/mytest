<%= form_for(@rleague) do |f| %>
  <% if @rleague.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rleague.errors.count, "error") %> prohibited this rleague from being saved:</h2>

      <ul>
      <% @rleague.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
