<h1>Listing douros</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @douros.each do |douro| %>
  <tr>
    <td><%= douro.name %></td>
    <td><%= link_to 'Show', douro %></td>
    <td><%= link_to 'Edit', edit_douro_path(douro) %></td>
    <td><%= link_to 'Destroy', douro, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Douro', new_douro_path %>
